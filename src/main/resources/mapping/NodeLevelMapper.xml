<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.inesv.mapper.NodeLevelMapper">

    <select id="getNodeBuyMin" resultType="decimal">
        SELECT MIN(amount_min) as minAcmount FROM t_node_level
    </select>

</mapper>